{
  "name": "deadline-signs-timeclock",
  "version": "1.0.0",
  "description": "Deadline Signs employee time clock app",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "build": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/status-bar": "^6.0.2",
    "@capacitor/keep-awake": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
